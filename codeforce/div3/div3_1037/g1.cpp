// 看什么看,以为我厉害吗？
// 我只是戾气很重,不厉害只需要114514分钟就能变厉害了
// 而你我的朋友只需要2.5年就可以了
#include<bits/stdc++.h>
#include<climits>


#define all(x) x.begin(),x.end()
#define pb(x) push_back(x)
#define i64 long long
#define fi first
#define se second

#define endl '\n'
#define mod3 998244353
#define mod7 1000000007
#define de(x) cerr << #x <<" "<<x <<" ";
#define deb(x) cerr << #x <<" "<<x <<endl;
using namespace std;

using pii = pair<int,int>;
using pll = pair<i64, i64>;
/***
 * 和一个可以 根据位置来维护 最小值的数据结构 线段树可以
 * 一个根据位置  维护中位数的 数据结构
 * 双指针 + 对顶堆，但是由于该循环移位不满足
 * 不满足扩张时 区域内答案增加，缩减区域时答案减小的性质
 */

void solve(){

}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int T;
    cin >> T;
    // 2025.07.17——23:54:47
/*
 *
 *
 *
*/
    while(T--){
        solve();
    }
    return 0;
}